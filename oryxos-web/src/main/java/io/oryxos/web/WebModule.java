package io.oryxos.web;

// WebModule: Spring MVC web layer with 10 REST endpoints under /api/v1.
// Includes session management, message handling, profile listing, tool listing, health/info
// endpoints.
// No auth (assumed internal network), no SSE streaming in core phase.
public class WebModule {}
