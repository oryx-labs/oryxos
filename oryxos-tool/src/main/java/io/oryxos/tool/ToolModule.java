package io.oryxos.tool;

// ToolModule: built-in tools (read_file, write_file, list_dir, shell, http_get, http_post).
// Also contains MCP client, ToolRegistry, and SandboxChecker (path/command/domain allowlists).
// SKILL.md is NOT a Tool — it belongs in ContextLoader (oryxos-core).
public class ToolModule {}
